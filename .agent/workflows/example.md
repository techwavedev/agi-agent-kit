---
description: Example workflow - copy and modify
---

1. Review directive in `directives/`

// turbo 2. Run script

```bash
python execution/_template.py --input .tmp/input.json --output .tmp/output.json
```

3. Verify output and deliver

> Use `// turbo` for auto-run steps, `// turbo-all` at top for all.
