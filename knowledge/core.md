# Core Knowledge — Always Loaded

> This file is injected into every agent session regardless of Qdrant availability.
> Keep it under 500 tokens. Only put non-negotiable, session-critical facts here.

## Project

- **Name:** (fill in project name)
- **Primary language:** (e.g. TypeScript / Python)
- **Framework / runtime:** (e.g. Next.js 14, FastAPI, Node 20)
- **Deployment target:** (e.g. Vercel, AWS, Docker)

## Tech Stack

- (List core dependencies / services here, one per line)

## Non-Negotiable Rules

- Never commit secrets or credentials to version control.
- All public API endpoints must validate and sanitize inputs.
- Follow the existing code style — do not introduce new conventions without discussion.
- Write tests for every new public function or API route.
